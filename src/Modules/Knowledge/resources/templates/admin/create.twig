{% extends "@admin/layout.twig" %}

{% block title "Créer une connaissance" %}

{% block content %}
  
  <h1>
    Créer une connaissance
  </h1>
  
  <section>
    <p>
      <a href="{{ path(routePrefix ~ '.index') }}">
        Retour
      </a>
    </p>
    
    <form action="{{ path(routePrefix ~ '.create') }}" method="post">
      {% include viewPath ~ '/form.twig' %}
      <div>
        <button type="submit">
          Créer
        </button>
      </div>
    </form>
  </section>

{% endblock %}
